# .env file template for API-based application
# Copy this to .env and fill in your actual values


# Optional: OpenAI API Key (if needed for fallback)
OPEN_API_KEY=your_openai_key_here

# Optional: Databricks credentials (if needed for direct access)
DATABRICKS_HOST=your_databricks_host
DATABRICKS_TOKEN=your_databricks_token
DATABRICKS_HTTP_PATH=your_databricks_http_path
